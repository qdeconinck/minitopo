checkers:
  tshark:
    - test: "filter"
      filter: "tcp.options.mptcp.subtype==5"
      validations:
        - name: "exact_value"
          target: 1
          desc: "When the default interface is a backup, MPRIO should be sent"
